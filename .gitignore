__pycache__/
*.pyc
.env
.venv
venv/
.DS_Store
# Keep data and models for this specific assignment delivery as requested
# usually we might ignore them, but the requirements say "Model files used by the tool"
# and "Sample input and output"
